# Document With Code Blocks

This document contains various code blocks.

## JavaScript Example

Here is some JavaScript:

```javascript
function hello() {
  console.log("Hello, world!");
  return true;
}
```

This text comes after the code block.

## Python Example

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

Some inline code: `const x = 42` and also `npm install github-global`.

## Shell Commands

```bash
npm install -g github-global
github-global init
github-global translate --lang en
```

End of document with `final_inline_code`.
